---
- hosts: all
  tasks:
  - name: Build Containers
    become: True
    shell:
      cmd: "docker-compose build"
      chdir: /home/hamza/Desktop/medicashare_vf/

  - name: Run Containers
    become: True
    shell:
      cmd: "docker-compose up"
      chdir: /home/hamza/Desktop/medicashare_vf/  

  - name: Deploy django&service to kubernetes
    command: kubectl apply -f deployment_django.yaml    

  - name: Deploy django&service to kubernetes
    command: kubectl apply -f deployment_django.yaml

  - name: Deploy postgres&service to kubernetes
    command: kubectl apply -f deployment_postgres.yml